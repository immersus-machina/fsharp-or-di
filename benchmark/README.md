# Benchmarks

```bash
dotnet run -c Release
```

Uses [BenchmarkDotNet](https://benchmarkdotnet.org/) against the [KeplerStation](../examples/KeplerStation/) example (66 registered functions with composition chaining).

Results are written to `BenchmarkDotNet.Artifacts/` in the working directory.
